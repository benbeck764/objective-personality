{
  "name": "objective-personality-functions",
  "version": "1.0.0",
  "description": "Azure functions for Objective Personality app.",
  "license": "UNLICENSED",
  "main": "dist/src/functions/**/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn run build",
    "start": "func start --typescript",
    "clean": "rimraf dist && rimraf node_modules"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.0.1",
    "@azure/storage-blob": "^12.16.0",
    "yup": "^1.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
