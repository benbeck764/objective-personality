import Typography from '@mui/material/Typography';

const Home = async () => {
  return (
    <>
      <Typography variant="h1" sx={{ color: { xs: 'red', xl: 'blue' } }}>
        Hello World
      </Typography>
    </>
  );
};

export default Home;
